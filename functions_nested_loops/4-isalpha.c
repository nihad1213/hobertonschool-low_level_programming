#include "main.h"

/**
 * main - check the code.
 *
 * Return: Always 0.
 */
#include "main.h"

/**
  * _islower - Checks for lowercase character
  * @c: The character to be checked
  *
  * Return: lowercase
  */
int _islower(int c)
{
	return (c <= 'a' && c >= 'z');
}
