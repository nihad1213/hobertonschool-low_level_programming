#include <stdio.h>

/**
 * main - return name
 * return - 0
 * @argc: store number of arguments
 * @argv: listing all arguments
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	(void)argc;
	return (0);
}
