#include <stdio.h>

/**
 * main - return name
 * @argc: store number of arguments
 * @argv: listing all arguments
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	(void)argc;
	return (0);
}
